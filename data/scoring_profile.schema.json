{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "scoring_profile.schema.json",
  "title": "ScoringProfile",
  "description": "Scoring profile for job offer ranking. Defines technology and location preferences used by Sourcing::ScoreStep to score job offers on a 0-100 scale.",
  "type": "object",
  "required": ["technology", "location"],
  "additionalProperties": false,
  "properties": {
    "technology": {
      "type": "object",
      "description": "Technology preferences for scoring job offers.",
      "required": ["primary", "secondary"],
      "additionalProperties": false,
      "properties": {
        "primary": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Required technologies. All primary technologies in an offer are required; missing any results in a penalty. Matching primary technologies do not earn additional points (they are baseline).",
          "examples": [["ruby", "rails"]]
        },
        "secondary": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Nice-to-have technologies. Each match with offer secondary or primary technologies earns +3 points. Subset of offer secondary technologies that match user's secondary list contributes to the score.",
          "examples": [["postgresql", "redis", "docker"]]
        }
      }
    },
    "location": {
      "type": "object",
      "description": "Location and work mode preferences for scoring job offers.",
      "required": ["preference"],
      "additionalProperties": false,
      "properties": {
        "preference": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "enum": ["remote", "hybrid", "on-site"]
          },
          "description": "Ordered list of preferred work modes, ranked from most to least preferred. Rank 0 is most preferred (100 points), rank 1 deducts 20 points, rank 2 deducts 40 points. Modes not in this list score 0 points.",
          "examples": [["remote", "hybrid", "on-site"]]
        },
        "hybrid": {
          "type": "object",
          "description": "Specific constraints and preferences for hybrid roles.",
          "required": ["remote_days_min_per_week"],
          "additionalProperties": false,
          "properties": {
            "remote_days_min_per_week": {
              "type": "number",
              "minimum": 0,
              "maximum": 5,
              "description": "Minimum acceptable remote days per week. Offers with fewer remote days score 0. Offers with more remote days deduct (5 - offer_days) * 10 points as a preference penalty.",
              "examples": [3]
            }
          }
        },
        "commute": {
          "type": "object",
          "description": "Optional commute filter. When present, every non-remote offer is geocoded and travel time from origin_city is computed; offers above max_minutes lose 100 points.",
          "required": ["origin_city", "max_minutes", "mode"],
          "additionalProperties": false,
          "properties": {
            "origin_city": {
              "type": "string",
              "minLength": 1,
              "description": "Home city used as the commute origin. Geocoded once via Mapbox.",
              "examples": ["Moisdon-la-Rivière"]
            },
            "max_minutes": {
              "type": "integer",
              "minimum": 1,
              "description": "Maximum acceptable one-way commute in minutes. Offers above this lose 100 points.",
              "examples": [90]
            },
            "mode": {
              "type": "string",
              "enum": ["driving", "cycling", "walking"],
              "description": "Mapbox Directions profile. Mapbox has no public transit routing.",
              "examples": ["driving"]
            }
          }
        }
      }
    }
  }
}
